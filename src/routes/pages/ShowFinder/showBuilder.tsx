import React from 'react';
import { Card } from '../../../designsystem';

const ShowBuilder = () => {
  return (
    <Card header='Book a Show'>
      <p>This is the show asdf builder page</p>
    </Card>
  );
};

export default ShowBuilder;
