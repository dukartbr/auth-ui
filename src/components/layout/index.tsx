import React from 'react';
import { Box, Row, Column, Stack } from '../../designsystem/index';

const Layout = ({ children }: any) => {
  return <Box bg='#0f0f0f'>{children}</Box>;
};

export default Layout;
